pipeline {
    agent any
    stages {
        stage('on Jenkins Server') {
            steps {
                sh '''
                echo "Hello,Jenkins is working"
                
                '''
            }  
        }
        stage('on App Server'){
            steps{
                sh'''
                
                echo "Hello,Jenkins is working"
                
                
                '''
            }
        }
    }
}